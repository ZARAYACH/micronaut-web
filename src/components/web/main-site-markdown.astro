---
import "@/styles/main-site-markdown.css";

type Props = {
  html?: string;
};

const { html = "" } = Astro.props;
---

{html && <div class="main-site-markdown" data-generated-docs set:html={html} />}
